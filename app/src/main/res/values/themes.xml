<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Beauty_app" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.Beauty_app" parent="Base.Theme.Beauty_app" >
        <item name="colorPrimary">@color/white</item>
        <item name="colorOnPrimary">@color/darkest_gray</item>
        <item name="colorSwitchThumbNormal">@color/semidark_gray</item>
        <item name="colorSurface">@color/light_gray</item>
    </style>
</resources>